# MongoDB Connection - Atlas Cloud Database
# Replace with your actual MongoDB connection string
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>
MONGODB_URI=mongodb+srv://your-username:your-password@your-cluster.mongodb.net/your-database

# Next.js Environment
NODE_ENV=development

# Site Configuration
NEXT_PUBLIC_SITE_URL=https://your-domain.com
NEXT_IMAGE_ALLOWED_HOSTS=bergaman.dev,www.bergaman.dev,i.scdn.co,image-cdn-ak.spotifycdn.com,image-cdn-fa.spotifycdn.com,open.spotify.com,www.google.com,github.com,avatars.githubusercontent.com,raw.githubusercontent.com,images.unsplash.com

MONGODB_DB=your-database-name
NEXTAUTH_SECRET=your-nextauth-secret
NEXTAUTH_URL=http://localhost:3000

# App Version (normally read from package.json at build time)
NEXT_PUBLIC_APP_VERSION=v2.9.0

# Email Configuration for Contact Form
EMAIL_USER=your-email@gmail.com
EMAIL_PASS="your-gmail-app-password"

# JWT Secret (güvenlik için güçlü bir değer kullanın)
JWT_SECRET=your-strong-jwt-secret-key-at-least-32-chars

# Admin kullanıcı bilgileri
# IMPORTANT: Change these values and keep them secret!
ADMIN_USERNAME=bergaman
# Required: bcrypt cost 12 hash generated from a unique strong password.
ADMIN_PASSWORD_HASH=$2b$12$replace-with-your-bcrypt-hash

# Rate Limiting
RATE_LIMIT_MAX=5
RATE_LIMIT_WINDOW_MS=900000

# Güvenlik ayarları
SECURE_COOKIES=true
SESSION_DURATION_HOURS=24

# API Rate Limiting
API_RATE_LIMIT=100
API_RATE_LIMIT_WINDOW_MS=60000

# Webhook security
WEBHOOK_SHARED_SECRET=replace-with-shared-webhook-secret
EMAIL_WEBHOOK_SECRET=replace-with-email-webhook-secret
GMAIL_WEBHOOK_SECRET=replace-with-gmail-webhook-secret

# CORS ayarları
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# Encryption key (32 karakter)
ENCRYPTION_KEY=32-character-encryption-key-here
