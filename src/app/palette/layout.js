export const metadata = {
  title: 'Palette Generator',
  description: 'Generate color schemes, lock the ones you like, and copy hex or a Tailwind snippet.',
  alternates: { canonical: 'https://bergaman.dev/palette' },
};

export default function PaletteLayout({ children }) {
  return children;
}
