export const metadata = {
  title: 'Focus Timer',
  description: 'A clean Pomodoro timer with focus and break cycles for deep work sessions.',
  alternates: { canonical: 'https://bergaman.dev/focus' },
};

export default function FocusLayout({ children }) {
  return children;
}
